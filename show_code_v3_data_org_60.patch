diff --git a/C64DevMachine_win.yyp/objects/obj_workspace_manager/Create_0.gml b/C64DevMachine_win.yyp/objects/obj_workspace_manager/Create_0.gml
index 826f307..96483c0 100644
--- a/C64DevMachine_win.yyp/objects/obj_workspace_manager/Create_0.gml
+++ b/C64DevMachine_win.yyp/objects/obj_workspace_manager/Create_0.gml
@@ -961,7 +961,7 @@ welcome_open          = !welcome_hide_checked;
 showcode_x    = ini_read_real("showcode", "x",    -1);
 showcode_y    = ini_read_real("showcode", "y",    50);
 showcode_w    = clamp(ini_read_real("showcode", "w",    420), 300, 900);
-showcode_rows = clamp(ini_read_real("showcode", "rows",  20),   5,  64);
+showcode_rows = clamp(ini_read_real("showcode", "rows",  20),   5,  60);
 showcode_open = (ini_read_real("showcode", "open", 1) == 1);
 showcode_mode = clamp(ini_read_real("showcode", "mode", 0), 0, 1);
 
diff --git a/C64DevMachine_win.yyp/scripts/scr_show_code_panel/scr_show_code_panel.gml b/C64DevMachine_win.yyp/scripts/scr_show_code_panel/scr_show_code_panel.gml
index 91a0d8d..521e199 100644
--- a/C64DevMachine_win.yyp/scripts/scr_show_code_panel/scr_show_code_panel.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_show_code_panel/scr_show_code_panel.gml
@@ -19,7 +19,7 @@
 /// for branches — so what the panel shows is what the VICE monitor will show.
 ///
 /// Resize: drag the LEFT or RIGHT edge for width, the BOTTOM edge for row
-/// count (5..64 lines).
+/// count (5..60 lines).
 ///
 /// The panel NEVER runs its own compile pass. scr_c64_do_update_addresses()
 /// already produces a full sizing-pass compile every time the graph changes,
@@ -266,8 +266,10 @@ function scr_show_code_build(_compiled) {
     if (!instance_exists(obj_workspace_manager)) { exit; }
 
     with (obj_workspace_manager) {
-        var _flat = [];
-        var _pc   = global.start_pc;
+        var _flat    = [];
+        var _pc      = global.start_pc;
+        var _pcstack = [];   // mirrors c64_new_program's org(-2)/org(-3) stack
+        var _run     = -1;   // index of the byte run currently being extended
 
         // Every label's address, internal ones included — this is what makes
         // VICE mode able to resolve a branch target.
@@ -296,13 +298,36 @@ function scr_show_code_build(_compiled) {
 
             // ORG relocates the program counter mid-stream — follow it so
             // every address below stays honest.
+            //
+            // -2 and -3 are NOT addresses: they are the compile chain's
+            // save/restore PC markers, matching c64_new_program's org().
+            // -2 pushes the current PC, -3 pops it back. Printing them as
+            // addresses is what produced the phantom "* = $FFFE" rows
+            // (-2 masked to 16 bits). They are bookkeeping, so they get a
+            // stack push/pop here and no row in the listing.
             if (_m == "org") {
                 var _o = _e[1];
                 if (is_string(_o)) {
                     _o = real(_o);
                 }
-                _pc = _o;
-                array_push(_flat, { kind:"org", key:"", name:"", pc:_pc, raw:_m, mnem:_m, val:0, lbl:"", sz:0, res:0, hasres:false });
+
+                if (_o == -2) {
+                    array_push(_pcstack, _pc);
+                    _run = -1;
+                    continue;
+                }
+
+                if (_o == -3) {
+                    if (array_length(_pcstack) > 0) {
+                        _pc = array_pop(_pcstack);
+                    }
+                    _run = -1;
+                    continue;
+                }
+
+                _pc  = _o;
+                _run = -1;
+                array_push(_flat, { kind:"org", key:"", name:"", owner:"", pc:_pc, raw:_m, mnem:_m, val:0, lbl:"", sz:0, res:0, hasres:false, count:0, vals:[], dkey:"" });
                 continue;
             }
 
@@ -319,18 +344,22 @@ function scr_show_code_build(_compiled) {
 
             // [2] is the owning node instance, or — on the branch/jump forms
             // that carry their target there instead — a string label.
-            var _key  = "";
-            var _name = "";
-            var _tstr = "";
+            var _key   = "";
+            var _name  = "";
+            var _owner = "";
+            var _tstr  = "";
             if (array_length(_e) > 2) {
                 var _tag = _e[2];
                 if (is_string(_tag)) {
                     _tstr = _tag;
                 } else if (_tag != noone) {
                     if (instance_exists(_tag)) {
-                        if (string_pos("MACRO_", string(_tag.node_type)) == 1) {
+                        // _owner is set for every node type; _key only for
+                        // macros, since only macros fold as macro groups.
+                        _owner = string(_tag.node_type);
+                        if (string_pos("MACRO_", _owner) == 1) {
                             _key  = string(_tag);
-                            _name = string(_tag.node_type);
+                            _name = _owner;
                         }
                     }
                 }
@@ -338,16 +367,52 @@ function scr_show_code_build(_compiled) {
 
             if (_m == "label") {
                 ds_map_set(_lblmap, _lbl, _pc);
+                _run = -1;
                 // Hidden scaffolding labels are recorded above and dropped
                 // here, which also stops them fragmenting a macro's run.
                 if (ds_map_exists(_userlbl, _lbl)) {
-                    array_push(_flat, { kind:"label", key:_key, name:_name, pc:_pc, raw:_m, mnem:_m, val:0, lbl:_lbl, sz:0, res:0, hasres:false });
+                    array_push(_flat, { kind:"label", key:_key, name:_name, owner:_owner, pc:_pc, raw:_m, mnem:_m, val:0, lbl:_lbl, sz:0, res:0, hasres:false, count:0, vals:[], dkey:"" });
                 }
                 continue;
             }
 
             if (_m == "byte" || _m == "byt" || _m == "byte_lab_lo" || _m == "byte_lab_hi") {
-                array_push(_flat, { kind:"byte", key:_key, name:_name, pc:_pc, raw:_m, mnem:"byte", val:_v, lbl:_lbl, sz:1, res:0, hasres:false });
+
+                // A byte whose operand is a label stays its own row — it is a
+                // pointer, not table filler, and it needs its own resolution.
+                if (_lbl != "") {
+                    _run = -1;
+                    array_push(_flat, { kind:"byte", key:_key, name:_name, owner:_owner, pc:_pc, raw:_m, mnem:"byte", val:_v, lbl:_lbl, sz:1, res:0, hasres:false, count:1, vals:[], dkey:"" });
+                    _pc += 1;
+                    continue;
+                }
+
+                // Plain table bytes accumulate into ONE "data" entry holding an
+                // array of values. An 8000-byte bitmap costs one struct and one
+                // real array here instead of 8000 structs, which is what keeps
+                // the rebuild cheap on a graph with several assets in it.
+                var _extend = false;
+                if (_run >= 0) {
+                    if (_flat[_run].key == _key && _flat[_run].owner == _owner) {
+                        if (_flat[_run].pc + _flat[_run].count == _pc) {
+                            _extend = true;
+                        }
+                    }
+                }
+
+                if (_extend) {
+                    array_push(_flat[_run].vals, _v);
+                    _flat[_run].count += 1;
+                    _flat[_run].sz    += 1;
+                } else {
+                    var _dname = "BYTE DATA";
+                    if (_owner != "") {
+                        _dname = _owner;
+                    }
+                    array_push(_flat, { kind:"data", key:_key, name:_dname, owner:_owner, pc:_pc, raw:"byte", mnem:"byte", val:0, lbl:"", sz:1, res:0, hasres:false, count:1, vals:[_v], dkey:"D:" + _owner + "@" + scr_show_code_hex(_pc, 4) });
+                    _run = array_length(_flat) - 1;
+                }
+
                 _pc += 1;
                 continue;
             }
@@ -365,7 +430,8 @@ function scr_show_code_build(_compiled) {
                 }
             }
 
-            array_push(_flat, { kind:"op", key:_key, name:_name, pc:_pc, raw:_m, mnem:_norm, val:_v, lbl:_lbl, sz:_sz, res:0, hasres:false });
+            _run = -1;
+            array_push(_flat, { kind:"op", key:_key, name:_name, owner:_owner, pc:_pc, raw:_m, mnem:_norm, val:_v, lbl:_lbl, sz:_sz, res:0, hasres:false, count:1, vals:[], dkey:"" });
             _pc += _sz;
         }
 
@@ -389,6 +455,38 @@ function scr_show_code_build(_compiled) {
     }
 }
 
+// =====================================================================
+// Emit the display rows for ONE flat entry at the given indent.
+// A byte table long enough to be worth hiding becomes its own collapsible
+// group; a short one just lists. Arrays are reference types in GML 2.3+,
+// so the pushes onto _out are seen by the caller.
+// =====================================================================
+function scr_show_code_emit(_out, _flat, _idx, _indent) {
+    var _ln = _flat[_idx];
+
+    if (_ln.kind != "data") {
+        array_push(_out, { kind:"line", idx:_idx, sub:0, indent:_indent, key:"", name:"", pc:_ln.pc, sz:_ln.sz, count:1, open:false });
+        return;
+    }
+
+    // Runs this short are not worth a fold — a 2-byte vector reads better inline.
+    if (_ln.count < 4) {
+        for (var _b = 0; _b < _ln.count; _b++) {
+            array_push(_out, { kind:"databyte", idx:_idx, sub:_b, indent:_indent, key:"", name:"", pc:_ln.pc + _b, sz:1, count:1, open:false });
+        }
+        return;
+    }
+
+    var _dopen = scr_show_code_is_open(_ln.dkey);
+    array_push(_out, { kind:"datagroup", idx:_idx, sub:0, indent:_indent, key:_ln.dkey, name:_ln.name, pc:_ln.pc, sz:_ln.count, count:_ln.count, open:_dopen });
+
+    if (_dopen) {
+        for (var _b2 = 0; _b2 < _ln.count; _b2++) {
+            array_push(_out, { kind:"databyte", idx:_idx, sub:_b2, indent:_indent + 10, key:"", name:"", pc:_ln.pc + _b2, sz:1, count:1, open:false });
+        }
+    }
+}
+
 // =====================================================================
 // FOLD — flat list -> visible rows, honouring the expand/collapse state.
 // Runs on a rebuild and on every expand/collapse click. Never on a plain
@@ -406,7 +504,7 @@ function scr_show_code_fold() {
             var _ln = showcode_flat[_i];
 
             if (_ln.key == "") {
-                array_push(_out, { kind:"line", idx:_i, key:"", name:"", pc:_ln.pc, sz:_ln.sz, count:1, open:false });
+                scr_show_code_emit(_out, showcode_flat, _i, 0);
                 _i += 1;
                 continue;
             }
@@ -423,11 +521,11 @@ function scr_show_code_fold() {
             }
 
             var _open = scr_show_code_is_open(_k);
-            array_push(_out, { kind:"group", idx:_i, key:_k, name:_ln.name, pc:_ln.pc, sz:_bytes, count:_cnt, open:_open });
+            array_push(_out, { kind:"group", idx:_i, sub:0, indent:0, key:_k, name:_ln.name, pc:_ln.pc, sz:_bytes, count:_cnt, open:_open });
 
             if (_open) {
                 for (var _g = _i; _g < _j; _g++) {
-                    array_push(_out, { kind:"child", idx:_g, key:_k, name:"", pc:showcode_flat[_g].pc, sz:showcode_flat[_g].sz, count:1, open:true });
+                    scr_show_code_emit(_out, showcode_flat, _g, 10);
                 }
             }
 
@@ -478,7 +576,7 @@ function scr_show_code_draw() {
         var _grab  = 5;    // edge hit zone, GUI px
 
         showcode_w    = clamp(showcode_w, 300, 900);
-        showcode_rows = clamp(showcode_rows, 5, 64);
+        showcode_rows = clamp(showcode_rows, 5, 60);
 
         var _pw = showcode_w;
 
@@ -495,10 +593,10 @@ function scr_show_code_draw() {
         var _py = floor(showcode_y);
 
         // Rows actually drawn: the setting, capped by what fits below the
-        // panel's current y. Dragging the panel down never destroys a 64-row
+        // panel's current y. Dragging the panel down never destroys a 60-row
         // setting — it just shows fewer until you drag it back up.
         var _fit  = floor((_gh - _py - _hdr_h - _pad_t - _pad_b - 6) / _row_h);
-        var _rows = clamp(showcode_rows, 1, max(1, min(64, _fit)));
+        var _rows = clamp(showcode_rows, 1, max(1, min(60, _fit)));
 
         var _body_h = _rows * _row_h;
         var _ph     = _hdr_h + _pad_t + _body_h + _pad_b;
@@ -569,12 +667,12 @@ function scr_show_code_draw() {
                     showcode_w = clamp(_mx - _px, 300, 900);
                 } else {
                     var _body_top = _py + _hdr_h + _pad_t;
-                    showcode_rows = clamp(round((_my - _pad_b - _body_top) / _row_h), 5, 64);
+                    showcode_rows = clamp(round((_my - _pad_b - _body_top) / _row_h), 5, 60);
                 }
 
                 _pw     = showcode_w;
                 _px     = floor(showcode_x);
-                _rows   = clamp(showcode_rows, 1, max(1, min(64, _fit)));
+                _rows   = clamp(showcode_rows, 1, max(1, min(60, _fit)));
                 _body_h = _rows * _row_h;
                 _ph     = _hdr_h + _pad_t + _body_h + _pad_b;
 
@@ -597,7 +695,7 @@ function scr_show_code_draw() {
                 _py = floor(showcode_y);
 
                 _fit    = floor((_gh - _py - _hdr_h - _pad_t - _pad_b - 6) / _row_h);
-                _rows   = clamp(showcode_rows, 1, max(1, min(64, _fit)));
+                _rows   = clamp(showcode_rows, 1, max(1, min(60, _fit)));
                 _body_h = _rows * _row_h;
                 _ph     = _hdr_h + _pad_t + _body_h + _pad_b;
                 if (!showcode_open) {
@@ -704,13 +802,15 @@ function scr_show_code_draw() {
             var _li = showcode_scroll + _r;
             if (_li >= _total) { break; }
 
-            var _row  = showcode_lines[_li];
-            var _ry   = _body_y + (_r * _row_h);
-            var _rhov = (showcode_resize == 0 && !showcode_dragging &&
-                         _mx >= _px + 6 && _mx < _px + _pw - 14 &&
-                         _my >= _ry && _my < _ry + _row_h);
+            var _row    = showcode_lines[_li];
+            var _ry     = _body_y + (_r * _row_h);
+            var _indent = _row.indent;
+            var _rhov   = (showcode_resize == 0 && !showcode_dragging &&
+                           _mx >= _px + 6 && _mx < _px + _pw - 14 &&
+                           _my >= _ry && _my < _ry + _row_h);
 
-            if (_row.kind == "group") {
+            // ---- collapsible rows: macro groups and byte tables ----
+            if (_row.kind == "group" || _row.kind == "datagroup") {
 
                 if (_rhov) {
                     draw_set_alpha(0.22);
@@ -728,11 +828,21 @@ function scr_show_code_draw() {
                     _sign = "[-]";
                 }
 
+                var _gtitle = _sign + " " + _row.name;
+                var _gcol   = make_color_rgb(255, 210, 80);
+                if (_row.kind == "datagroup") {
+                    // Byte tables get their span in the title — the thing you
+                    // actually want to know about a data block you cannot see.
+                    _gtitle = _sign + " " + _row.name + "  $" + scr_show_code_hex(_row.pc, 4)
+                            + "-$" + scr_show_code_hex(_row.pc + _row.count - 1, 4);
+                    _gcol   = make_color_rgb(180, 170, 210);
+                }
+
                 draw_set_color(make_color_rgb(150, 150, 160));
-                draw_text_transformed(_col_addr, _ry, "." + scr_show_code_hex(_row.pc, 4), 1.0, 1.0, 0);
+                draw_text_transformed(_col_addr + _indent, _ry, "." + scr_show_code_hex(_row.pc, 4), 1.0, 1.0, 0);
 
-                draw_set_color(make_color_rgb(255, 210, 80));
-                draw_text_transformed(_col_byte, _ry, _sign + " " + _row.name, 1.0, 1.0, 0);
+                draw_set_color(_gcol);
+                draw_text_transformed(_col_byte + _indent, _ry, _gtitle, 1.0, 1.0, 0);
 
                 draw_set_halign(fa_right);
                 draw_set_color(make_color_rgb(110, 130, 150));
@@ -744,11 +854,26 @@ function scr_show_code_draw() {
 
             var _ln = showcode_flat[_row.idx];
 
-            var _indent = 0;
-            var _tint   = make_color_rgb(210, 220, 230);
-            if (_row.kind == "child") {
-                _indent = 10;
-                _tint   = make_color_rgb(160, 200, 220);
+            // ---- one byte out of an expanded (or short) table ----
+            if (_row.kind == "databyte") {
+                var _bv = _ln.vals[_row.sub];
+
+                draw_set_color(make_color_rgb(150, 150, 160));
+                draw_text_transformed(_col_addr + _indent, _ry, "." + scr_show_code_hex(_row.pc, 4), 1.0, 1.0, 0);
+
+                if (showcode_mode == 0) {
+                    draw_set_color(make_color_rgb(190, 190, 120));
+                    draw_text_transformed(_col_byte + _indent, _ry, scr_show_code_hex(_bv, 2), 1.0, 1.0, 0);
+                }
+
+                draw_set_color(make_color_rgb(180, 170, 210));
+                draw_text_transformed(_col_text + _indent, _ry, ".BYTE $" + scr_show_code_hex(_bv, 2), 1.0, 1.0, 0);
+                continue;
+            }
+
+            var _tint = make_color_rgb(210, 220, 230);
+            if (_indent > 0) {
+                _tint = make_color_rgb(160, 200, 220);
             }
             if (_ln.kind == "label") {
                 _tint = make_color_rgb(120, 230, 140);
