diff --git a/C64DevMachine_win.yyp/scripts/scr_node_tooltip_text/scr_node_tooltip_text.gml b/C64DevMachine_win.yyp/scripts/scr_node_tooltip_text/scr_node_tooltip_text.gml
index ab3a325..e6de229 100644
--- a/C64DevMachine_win.yyp/scripts/scr_node_tooltip_text/scr_node_tooltip_text.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_node_tooltip_text/scr_node_tooltip_text.gml
@@ -23,12 +23,20 @@ function scr_node_tooltip_text(_node_type) {
         "MACRO_PRINT": {
             title: "PRINT",
             lines: [
-                "Prints fixed inline text to screen RAM at a given",
-                "row/column, PETSCII-uppercase, with an optional",
-                "colour fill and screen-base override.",
+                "Prints text to screen RAM at a given row/column.",
                 "",
-                "For scrolling text use TXT SCROLL; for numeric",
-                "values (vars/registers) use PRINT EXT."
+                "SOURCE: INLINE fixed text, or an ASSET (TEXT_DATA)",
+                "read through a start/end byte window. Either end of",
+                "that window can be a literal offset or a named VAR,",
+                "so the printed slice can shift at runtime - e.g.",
+                "paging through a longer block of asset text.",
+                "",
+                "COL sets the single text colour (0-15) - not a fill,",
+                "just the colour the characters print in.",
+                "PRE-CLEAR optionally wipes screen RAM before printing.",
+                "",
+                "H/V alignment can auto-left/centre/right and",
+                "top/mid/bottom the text instead of a fixed X/Y."
             ]
         },
 
