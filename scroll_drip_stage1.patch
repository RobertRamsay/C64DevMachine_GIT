diff --git a/C64DevMachine_win.yyp/objects/obj_c64_node/Draw_0.gml b/C64DevMachine_win.yyp/objects/obj_c64_node/Draw_0.gml
index 6201bf4..0491129 100644
--- a/C64DevMachine_win.yyp/objects/obj_c64_node/Draw_0.gml
+++ b/C64DevMachine_win.yyp/objects/obj_c64_node/Draw_0.gml
@@ -144,7 +144,7 @@ if (height_dirty) {
         var _sc_rows     = 10; // +1 base for the always-visible CLR UNUSED row
         var _sc_src_mode = (array_length(instructions[0]) > 6 && is_real(instructions[0][6])) ? real(instructions[0][6]) : 0;
         if (_sc_src_mode == 1) {
-            _sc_rows += 3; // TILESET row + MAP IDX row + BASE ADDR row
+            _sc_rows += 4; // TILESET row + MAP IDX row + BASE ADDR row + MODE row
             var _sc_map_idx_mode = (array_length(instructions[0]) > 11 && is_real(instructions[0][11])) ? real(instructions[0][11]) : 0;
             if (_sc_map_idx_mode == 1) {
                 _sc_rows += 1; // SETMAP info row
diff --git a/C64DevMachine_win.yyp/scripts/scr_compile_chain/scr_compile_chain.gml b/C64DevMachine_win.yyp/scripts/scr_compile_chain/scr_compile_chain.gml
index 1a0a812..9a7a88d 100644
--- a/C64DevMachine_win.yyp/scripts/scr_compile_chain/scr_compile_chain.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_compile_chain/scr_compile_chain.gml
@@ -2882,6 +2882,21 @@ case "MACRO_SCROLL": {
     if (array_length(_id.instructions[0]) > 12 && is_string(_id.instructions[0][12])) {
         _mm_map_var = string(_id.instructions[0][12]);
     }
+
+    // [13] mode: 0 = FULL (existing full-flatten, default), 1 = DRIP
+    // (60-column runway, decode-ahead into the off-buffer during idle
+    // ticks, real memory saving on wide maps). Stage 1 only — the shell.
+    // DRIP falls back to identical FULL behaviour below until the actual
+    // decode-ahead mechanism is built; this just lets the toggle exist and
+    // be tested without silently doing the wrong thing in the meantime.
+    var _mm_mode = 0;
+    if (array_length(_id.instructions[0]) > 13 && is_real(_id.instructions[0][13])) {
+        _mm_mode = real(_id.instructions[0][13]);
+    }
+    if (_mm_mode == 1) {
+        show_debug_message("MACRO_SCROLL(META): DRIP mode selected but not yet implemented — falling back to FULL flatten this build.");
+    }
+
     // Declared here (not inside the src_mode branch below) since the loader
     // call sites and the setmap-routine gate need to read these regardless
     // of which branch actually set them.
diff --git a/C64DevMachine_win.yyp/scripts/scr_node_draw_macro_scroll/scr_node_draw_macro_scroll.gml b/C64DevMachine_win.yyp/scripts/scr_node_draw_macro_scroll/scr_node_draw_macro_scroll.gml
index 3d4e615..90aa74a 100644
--- a/C64DevMachine_win.yyp/scripts/scr_node_draw_macro_scroll/scr_node_draw_macro_scroll.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_node_draw_macro_scroll/scr_node_draw_macro_scroll.gml
@@ -155,6 +155,18 @@ function scr_node_draw_macro_scroll(_draw_x, _y, _cam_x, _cam_y, _cam_zoom) {
         draw_text(_px + 100, _ly, "$" + string_upper(decimal_to_hex(_mm_base_addr)));
         _ly += _lh;
 
+        var _mm_mode = (array_length(instructions[0]) > 13 && is_real(instructions[0][13])) ? real(instructions[0][13]) : 0;
+        draw_set_color(c_gray);
+        draw_text(_px, _ly, "MODE:");
+        if (_mm_mode == 0) {
+            draw_set_color(c_aqua);
+            draw_text(_px + 76, _ly, "FULL");
+        } else {
+            draw_set_color(c_orange);
+            draw_text(_px + 76, _ly, "DRIP (soon)");
+        }
+        _ly += _lh;
+
         if (_mm_map_idx_mode == 1) {
             draw_set_color(c_gray);
             draw_text(_px, _ly, "SETMAP:");
diff --git a/C64DevMachine_win.yyp/scripts/scr_node_spawn/scr_node_spawn.gml b/C64DevMachine_win.yyp/scripts/scr_node_spawn/scr_node_spawn.gml
index 3a1ac8e..36200b9 100644
--- a/C64DevMachine_win.yyp/scripts/scr_node_spawn/scr_node_spawn.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_node_spawn/scr_node_spawn.gml
@@ -182,7 +182,8 @@ case "LABEL": {
 		case "MACRO_SCROLL":
             _n.node_title   = "MAP H SCROLL";
             // [6]=src_mode(0=MAP_DATA,1=META_TILESET) [7]=tileset_name [8]=map_index [9]=base_addr [10]=clamp_blank [11]=map_idx_mode(0=LIT,1=VAR) [12]=map_idx_var_name
-            _n.instructions = [["MACRO_SCROLL", 0, 25, 1, 1, 1, 0, "", 0, 0x4000, 1, 0, ""]];
+            // [6]=src_mode(0=MAP_DATA,1=META_TILESET) [7]=tileset_name [8]=map_index [9]=base_addr [10]=clamp_blank [11]=map_idx_mode(0=LIT,1=VAR) [12]=map_idx_var_name [13]=mode(0=FULL,1=DRIP)
+            _n.instructions = [["MACRO_SCROLL", 0, 25, 1, 1, 1, 0, "", 0, 0x4000, 1, 0, "", 0]];
             _n.pc_address   = global.start_pc;
             _n.scroll_alias = "scr" + string(instance_number(obj_c64_node));
             with (_n) { event_user(0); }
diff --git a/C64DevMachine_win.yyp/scripts/scr_node_step_macro_scroll/scr_node_step_macro_scroll.gml b/C64DevMachine_win.yyp/scripts/scr_node_step_macro_scroll/scr_node_step_macro_scroll.gml
index 213d308..b8c3ec9 100644
--- a/C64DevMachine_win.yyp/scripts/scr_node_step_macro_scroll/scr_node_step_macro_scroll.gml
+++ b/C64DevMachine_win.yyp/scripts/scr_node_step_macro_scroll/scr_node_step_macro_scroll.gml
@@ -65,12 +65,13 @@ var _ry = [
     }
 
     // Rows below JSR LEFT/RIGHT (rows 4,5) — SOURCE at row 6, TILESET at
-    // row 7, MAP IDX at row 8, BASE ADDR at row 9. Must mirror the draw
-    // function's _ly sequence.
+    // row 7, MAP IDX at row 8, BASE ADDR at row 9, MODE at row 10. Must
+    // mirror the draw function's _ly sequence.
     var _ly_source    = (_ly0 + _lh * 6 - _cam_y) / _cam_zoom;
     var _ly_tileset   = (_ly0 + _lh * 7 - _cam_y) / _cam_zoom;
     var _ly_mapidx    = (_ly0 + _lh * 8 - _cam_y) / _cam_zoom;
     var _ly_baseaddr  = (_ly0 + _lh * 9 - _cam_y) / _cam_zoom;
+    var _ly_mode      = (_ly0 + _lh * 10 - _cam_y) / _cam_zoom;
 
     // Pad missing slots individually with their real defaults — never
     // blind-zero index [9] (base_addr), a real $0000 target silently
@@ -82,16 +83,17 @@ var _ry = [
     if (array_length(instructions[0]) < 11) { array_push(instructions[0], 1); }
     if (array_length(instructions[0]) < 12) { array_push(instructions[0], 0); }
     if (array_length(instructions[0]) < 13) { array_push(instructions[0], ""); }
+    if (array_length(instructions[0]) < 14) { array_push(instructions[0], 0); }
     var _mm_src_mode = is_real(instructions[0][6]) ? real(instructions[0][6]) : 0;
 
     // CLR UNUSED sits right after whatever the draw function drew last —
-    // row 7 is one past SOURCE's own row (6), +3 when META_TILESET is
-    // active (TILESET/MAP IDX/BASE ADDR), +1 more if MAP IDX is in VAR
-    // mode (SETMAP info row), +1 more if that tileset carries a stamp
+    // row 7 is one past SOURCE's own row (6), +4 when META_TILESET is
+    // active (TILESET/MAP IDX/BASE ADDR/MODE), +1 more if MAP IDX is in
+    // VAR mode (SETMAP info row), +1 more if that tileset carries a stamp
     // override. Must mirror draw exactly.
     var _mm_clamp_row_offset = 7;
     if (_mm_src_mode == 1) {
-        _mm_clamp_row_offset += 3;
+        _mm_clamp_row_offset += 4;
         var _mm_map_idx_mode_chk = is_real(instructions[0][11]) ? real(instructions[0][11]) : 0;
         if (_mm_map_idx_mode_chk == 1) {
             _mm_clamp_row_offset += 1;
@@ -212,5 +214,17 @@ var _ry = [
             obj_workspace_manager.is_entering_text     = true;
             return;
         }
+
+        // MODE row — toggle FULL/DRIP, index [13]
+        if (_in_col && _mgy >= _ly_mode && _mgy < _ly_mode + _lh_g) {
+            var _mm_cur_mode = is_real(instructions[0][13]) ? real(instructions[0][13]) : 0;
+            if (_mm_cur_mode == 0) {
+                instructions[0][13] = 1;
+            } else {
+                instructions[0][13] = 0;
+            }
+            scr_c64_update_addresses();
+            return;
+        }
     }
 }
\ No newline at end of file
